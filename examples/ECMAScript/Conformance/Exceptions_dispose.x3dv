#X3D V3.0 utf8
# (c) SenseGraphics 2010

PROFILE Immersive

# This example shows the function of the dispose function which will
# manually
# Markus note, it seems like this conformance test is not complete, dispose does not exist, should it? What does x3d spec say.

DEF SC Script {
    url ["ecmascript:
        function initialize() {
          v = Browser.currentScene.createNode('Shape');
          v.dispose();
          Browser.print( v )
        }
    "]
}

